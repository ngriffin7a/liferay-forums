/* SPDX-License-Identifier: LGPL-2.1-or-later */

/* The navigation is rendered server-side as a standard Clay "menubar-primary"
   nav (see index.html). No client-side script is needed: submenus use the
   Clay "nav-stacked show" markup and stay expanded, so the menu is fully
   functional without React. */
